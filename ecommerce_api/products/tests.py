# from django.test import TestCase

# # Create your tests here.
# import requests  # type: ignore

# # Assuming you have obtained the access token somehow
# access_token = "<your_access_token>"
# headers = {"Authorization": f"Bearer {access_token}"}

# # Make a POST request to create a category
# category_data = {"name": "New Category"}  # Example category data
# response = requests.post("http://127.0.0.1:8000/api/categories/", json=category_data, headers=headers)

# print(response.json())